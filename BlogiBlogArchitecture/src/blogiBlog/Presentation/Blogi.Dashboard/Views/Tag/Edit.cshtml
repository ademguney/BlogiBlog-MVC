@model Blogi.Dashboard.Models.TagEditViewModel
@{
    ViewData["Title"] = "Tag Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="xp-breadcrumbbar">
            <div class="row">
                <div class="col-md-6 col-lg-6">
                    <h4 class="xp-page-title">
                        <a asp-controller="Dashboard" asp-action="Home" style="color: #8A98AC"><i class="icon-home"></i> @Localize("page_default_label_home")</a>
                    </h4>
                </div>
                <div class="col-md-6 col-lg-6">
                    <div class="xp-breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item">
                                <a asp-controller="Tag" asp-action="Home"><i class="icon-home"></i></a>
                            </li>
                            <li class="breadcrumb-item active" aria-current="page">
                                <a asp-controller="Tag" asp-action="Home">@Localize("page_default_back_to_list")</a>
                            </li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<br />

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <form asp-controller="Tag" asp-action="Edit" method="post">
                     <input type="hidden" asp-for="Tag.Id" name="Tag.Id" />
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>                    
                     <fieldset class="form-group">
                        <div class="row">
                            <div class="col-form-label col-sm-4 pt-0">@Localize("page_default_label_language")</div>
                            <div class="col-sm-8">
                                <select asp-for="Tag.LanguageId" class="form-control" required>
                                    <option selected="selected" value="0">@Localize("page_default_label_select_language")</option>
                                    @foreach (var item in Model.LanguageList)
                                    {
                                            <option value="@item.Id">@item.Name</option>
                                    }
                                </select>
                            </div>
                        </div>
                    </fieldset>
                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label">@Localize("page_tag_label_tag_name")</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" asp-for="Tag.Name" required>
                            <span class="text-danger" asp-validation-for="Tag.Name"></span>
                        </div>
                    </div>
                       <div class="form-group row">
                        <label class="col-sm-4 col-form-label">@Localize("page_post_label_url")</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" asp-for="Tag.Slug" required>
                            <span class="text-danger" asp-validation-for="Tag.Slug"></span>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-block btn-warning"> @Localize("page_default_button_update")<i class="mdi mdi-plus ml-2"></i></button>
                </form>
            </div>
        </div>
    </div>
</div>